/* Generated by compiler-practice/codegen.py. FP32 CPU kernels. */
#include <stddef.h>
#define WIDTH ((size_t)128)
#define BATCH ((size_t)1)

void dense_relu(const float *restrict X, const float *restrict W, const float *restrict bias, float *restrict Y) {
  for (size_t b = 0; b < BATCH; ++b) {
    for (size_t row = 0; row < WIDTH; ++row) {
      float sum = 0.0f;
      for (size_t col = 0; col < WIDTH; ++col) {
        sum += X[b * WIDTH + col] * W[row * WIDTH + col];
      }
      float value = sum + bias[row];
      Y[b * WIDTH + row] = value > 0.0f ? value : 0.0f;
    }
  }
}
